import { Layout } from "../../components/Layout/Layout"
import { Timer } from "../../components/Timer/Timer"

export const Home = () => {
    return (
        <Layout>
            <Timer />
        </Layout>
    )
}